namespace Quantum {
  using System;

  /// <summary>
  /// Obsolete script. Navmesh editor gizmos are now rendered with the map.
  /// </summary>
  [Obsolete("Navmesh editor gizmos are now rendered with the map")]
  public class QuantumNavMeshDebugDrawer : QuantumMonoBehaviour {
  }
}