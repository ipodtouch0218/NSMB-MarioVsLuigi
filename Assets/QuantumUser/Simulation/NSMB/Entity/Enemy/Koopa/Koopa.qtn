component Koopa {
	asset_ref<EntityPrototype> SpawnWhenStomped;
	bool DontWalkOfLedges;
	bool IsSpiny;
	FP Speed;
	FP KickSpeed;
	
	byte Combo;
	FP CurrentSpeed;
	bool IsInShell;
	bool IsFlipped;
	bool IsKicked;
	ushort WakeupFrames;
	byte IgnoreOwnerFrames;
}

event KoopaEnteredShell {
	nothashed Frame Frame;
	EntityRef Entity;
}