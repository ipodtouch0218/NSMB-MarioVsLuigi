using Photon.Deterministic;

namespace Quantum {
    public class CommandToggleReady : DeterministicCommand {
        public override void Serialize(BitStream stream) {
            // Sorry, nothing.
        }
    }
}