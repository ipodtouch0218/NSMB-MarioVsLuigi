import enum LiquidType(byte);

component Liquid {
	LiquidType LiquidType;
	int WidthTiles;
	FP HeightTiles;

	[ExcludeFromPrototype, FreeOnComponentRemoved] list<EntityRef> SplashedEntities;
}

signal OnTryLiquidSplash(EntityRef entity, EntityRef liquid, bool exit, bool* doSplash);

event LiquidSplashed {
	entity_ref Entity;
	nothashed FP Force;
	nothashed FPVector2 Position;
	bool Exit;
}